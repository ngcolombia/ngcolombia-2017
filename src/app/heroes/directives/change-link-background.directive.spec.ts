import { ChangeLinkBackgroundDirective } from './change-link-background.directive';

describe('ChangeLinkBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeLinkBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
